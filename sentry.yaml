ingress:
  enabled: true  
  hostname: sentry.local 

sentry:
  singleOrganization: false
  worker:
    replicas: 1

system:
  url: "https://sentry.local"
  adminEmail: "admin@sentry.local"
  public: true  

postgresql:
  enabled: true
 